# ============================================================
# .gitignore —— 这个仓库里"绝对不能提交"的东西比平时多，
# 因为它们不是"脏文件"，而是**授权问题**。改动前请先看懂下面的说明。
# ============================================================

# ---- 编译产物 ----
bin/
obj/
.vs/

# WebView2 运行时会自动在 exe 旁边建这个目录（里面是本机浏览器缓存）
*.WebView2/

# ---- ★ 授权红线：这几样一旦进仓库就是侵权，别删这几行 ----
#
# ① Live2D Cubism Core —— Live2D 公司的专有授权文件，不允许随项目分发。
#    页面已经做了兜底：本地没有这个文件时会自动从 Live2D 官方 CDN 取。
web/vendor/live2dcubismcore.min.js

# ② Live2D 模型资产 —— 各人自备（购买或自制），不随项目分发。
#    正常放在工程目录之外（config.json 里指的是绝对路径），这里是防御性写法：
#    万一有人把模型目录挪进工程里，也不会被误提交。
绒绒/
models/

# ③ 语音文件 —— 从游戏里拆出来的音频不能公开分发。
voice/
voices/

# ---- 其它 ----
*.user
