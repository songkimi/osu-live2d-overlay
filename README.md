# osu-live2d-overlay

给 osu!mania 用的 Live2D 悬浮窗：角色待在游戏画面旁边，随连击和断连击切表情、说语音、对口型。

> 早期开发版本，配置项和界面都还可能变。
>
> **没接触过编程也没关系** —— 下面的步骤是照着做就能完成的，
> 配置只需要用记事本改两行。遇到问题先看「出问题了？对照这里」。

---

## 现在能做什么

- ✅ 透明、置顶、**点击穿透、不抢焦点** —— 硬要求是"绝不干扰打歌"
- ✅ 位置和大小可调，拖动移动、右下角缩放
- ✅ 待机动作循环；跨过设定的连击数时切表情
- ✅ 断连击按"断之前手里有多少连击"分两档反应，**低于门槛完全不反应**
  （玩得菜的时候一直被安慰只会更难受，所以宁可不理）
- ✅ 语音与表情**完全解耦**：同一件事分别决定"演什么"和"说什么"；
  一个情绪里放多个语音会随机挑，且不会连着重复同一句
- ✅ 说话时嘴巴跟着开合，说完自动闭上交还给待机动作
- ✅ 目光跟随鼠标
- ✅ 行为**全部由 `config.json` 控制**：换模型、换表情、换语音、改阈值都不用改代码

---

## 怎么跑起来

### 你需要准备什么

| 需要什么 | 必须有吗 | 说明 |
|---|---|---|
| 一台 Windows 电脑 | 必须 | |
| **.NET 10 SDK** | 必须 | 到 [dotnet.microsoft.com/download](https://dotnet.microsoft.com/download) 下载安装，一路下一步就行 |
| **tosu** | 必须 | 到 [tosu 的 Releases 页面](https://github.com/tosuapp/tosu/releases) 下载，解压运行。它负责读取游戏数据，**必须先启动它** |
| **osu!**（stable 或 lazer） | 必须 | 你玩的那个 |
| **一个 Live2D 模型** | 必须 | Cubism 3 以上的模型（自己买或自己做）。见下面「模型」 |
| **语音文件** | 可选 | 没有也能用，只是角色不说话 |
| `live2dcubismcore.min.js` | 必须 | Live2D 的官方核心文件。**程序会自己从官方 CDN 下载**，通常不用管 |

### 四个步骤

**第 1 步：装 .NET 10 SDK**
下载后双击安装，一路"下一步"。装完不用管它。

**第 2 步：启动 tosu**
解压 tosu，双击运行。它会打开一个网页控制台（默认 `127.0.0.1:24050`），说明已经好了。
**之后每次都要先开它。**

**第 3 步：改配置**
打开本项目的 `config.json`，至少要改「模型」里的两项。详细说明见下面的**「配置说明」**。

**第 4 步：运行**

1. 用文件资源管理器打开 `osu-live2d-overlay` 这个文件夹
2. 点一下**最上面的地址栏**（显示路径的那一条），把里面的内容全删掉，输入 `cmd`，按回车
3. 会弹出一个黑色窗口，在里面输入下面这行，按回车：

   ```
   dotnet run
   ```

   第一次运行要联网下载依赖，会慢一两分钟；之后再运行就快了。

关掉它：按 `Ctrl+Alt+Q`，或者关掉那个黑窗口。

### 看不到悬浮窗？

把**游戏的显示模式切换一次**（全屏 → 无边框，再切回来），切一次就好，以后都不会再出现。

原因是游戏用"独占全屏"的时候会盖住所有悬浮窗，这是 Windows 的机制，不是插件的问题。

---

## 配置说明

配置文件是 **`config.json`**，就在 `osu-live2d-overlay` 文件夹里。
用**记事本**打开就能改（右键 → 打开方式 → 记事本）。

改完**重开程序**生效 —— **不需要重新编译**。

### 先认识这个文件

- 以 `_` 开头的项（比如 `"_从哪开始"`）都是给人看的**注释**，不用动它们
- JSON 的规矩就三条：
  1. 文字要用**英文双引号** `"` 包起来（不能用中文引号）
  2. 每一项之间用**英文逗号** `,` 隔开
  3. **最后一项后面不要加逗号**
- 改错了不要紧，程序读不懂配置时会用默认值继续跑，不会崩

### 最少要改两个地方

| 要改的 | 改成什么 |
|---|---|
| `模型` → `目录` | 你的模型文件夹完整路径，例如 `D:\\Live2D\\我的角色` |
| `模型` → `入口` | 那个文件夹里的 `.model3.json` 文件名，例如 `我的角色.model3.json` |

> ⚠️ **路径里的反斜杠要写两个**：`D:\Live2D` 要写成 `D:\\Live2D`。
> 这是 JSON 的转义规则，少写一个程序就读不到，而且不会报错、只是什么都没发生。

改完这两处，角色就能显示出来了。**其余的都可以先不动。**

### 怎么拿到一个文件夹的完整路径

1. 用文件资源管理器打开那个文件夹
2. 点一下**最上面的地址栏**
3. 路径会变成选中状态，按 `Ctrl+C` 复制

复制出来长这样：`D:\Live2D\我的角色`
填进配置时要写成：`D:\\Live2D\\我的角色`

### 模型

```json
"模型": {
  "目录": "D:\\Live2D\\我的角色",     ← 模型文件夹（必填）
  "入口": "我的角色.model3.json",     ← 文件夹里的入口文件（必填）
  "待机动作": "待机.motion3"          ← 待机时循环播哪个动作（可留空）
}
```

**怎么确认我的模型能用？** 打开模型文件夹，看**有没有 `.model3.json` 结尾的文件**：

- 有 → 就是它，把它填到「入口」
- 没有 → 这个模型不是 Cubism 格式（可能是 Live2D 老版本或别的软件做的），本程序用不了

### 「标识」是什么（先看这段，后面全用它）

程序启动时会**扫描模型文件夹**，把里面的 `.exp3.json`（表情）和 `.motion3.json`（动作）都找出来，
给每一个起一个**标识**——就是**文件名去掉 `.json`**：

| 模型文件夹里的文件 | 标识 |
|---|---|
| `exp/2.exp3.json` | `2.exp3` |
| `待机.motion3.json` | `待机.motion3` |
| 官方模型里已注册的动作组 `Idle` | `Idle`（保持原组名，因为一组里可能有多个文件） |

**下面所有配置填的都是标识。** 比如 `"待机动作": "待机.motion3"`。

**想知道自己的模型有哪些标识？** 把 `调试模式` 改成 `true`，启动后打开
`%TEMP%\osu-live2d-overlay\debug.log`，里面会写明扫描结果（表情多少个、动作多少个）。

> 为什么要这样：模型里的表情文件名常常是 `1.exp3.json`、`2.exp3.json` 这种编号，
> 谁都看不出哪个是什么。程序会去读模型自带的 `cdi3` 文件（Live2D 官方标准的"显示辅助文件"，
> 专门给参数配人话名字），把能翻译的都翻译出来 —— 绒绒的 17 个表情全部能翻译：
> `3.exp3` 是「星星眼」、`7.exp3` 是「害羞」、`16.exp3` 是「吐舌」……
> 日志里会打出「标识 → 名字」的清单，照着填就行。
> 翻译不出来的就显示标识本身（官方模型一个表情要改十几个参数，没法用一个词概括，
> 而它们的文件名本身就是 `Angry`、`Smile` 这种看得懂的词）。

### 三层：谁管"什么时候该退场"

角色脸上的表情**不是**"同一时刻只有一个"，而是三层同时挂着、每帧合并出来的：

| 层 | 谁决定 | 什么时候退场 |
|---|---|---|
| **常驻组件** | 你在配置里勾的 | 一直在，除非你改配置 |
| **常态区间** | 连击落在哪一档 | 换档时旧的淡出、新的淡入 |
| **连击触发 / 失误触发** | 阈值到了 | 播完「表情持续时间毫秒」后淡出 |

所以"打歌时一直吐舌头（常驻）＋ 连击上来变星星眼（常态）＋ 断连击害羞一下（瞬时）"
是**三层叠在一起**的结果，不是互相顶掉。

既然能叠，就得有个规矩：**同一个参数被两层同时改了，上面那层赢。**
好在模型里的参数大多各管各的（绒绒那 17 个表情就是 17 个互不相干的开关），
所以多数情况下三层互不打扰、各演各的。

### 常态区间（连击在某个范围内时，一直保持的表情）

```json
"常态区间": [
  { "最小": 0,   "最大": 49, "表情": "1.exp3" },
  { "最小": 50,  "最大": 99, "表情": "3.exp3" },
  { "最小": 100,             "表情": "13.exp3" }
]
```

- **不写「最大」= 以上**（第三档就是"100 以上"）
- 每一档填一个表情标识
- 区间**不要重叠**（重叠时运行时会用靠前那档，但那是兜底，不是设计）
- 整段留空 = 不切常态表情，角色一直保持原样

### 常驻组件（一直挂着的部件）

有些模型有"猫耳""水印""尾巴"这类可以开关的部件（它们同样是 `.exp3.json`）。
想让哪些一直开着，就把它们的标识列在这里：

```json
"常驻组件": [ "猫耳.exp3", "尾巴.exp3" ]
```

- **列出来 = 开着，没列 = 关着**
- 不同模型的部件名字完全不一样，所以只能靠你自己看着填（日志里会打出「标识 → 中文名」清单）
- 留空 `[]` = 一个都不开

### 语音（没有就关掉）

```json
"语音": {
  "启用": false,              ← 没有语音文件就写 false
  "目录": "D:\\我的语音",      ← 语音文件夹
  "音量": 0.8,                 ← 0 到 1 之间
  "最小间隔毫秒": 300,          ← 两句语音之间至少隔多久，防止连发
  "情绪": [
    { "名称": "高兴", "文件": [ "加油.wav", "最喜欢了.wav" ] },
    { "名称": "伤心", "文件": [ "可惜.wav" ] }
  ]
}
```

- 一个情绪里写**多个文件**时，每次会随机挑一个，而且不会连着重复同一句
- **文件多的时候用省事写法**：不写 `文件` 这一项，直接在你的语音文件夹里建一个叫「高兴」的文件夹，
  把音频全丢进去 —— 程序会自动收下这个文件夹里的所有音频
- 支持的格式：`.wav` `.mp3` `.ogg` `.m4a` `.aac` `.flac` `.opus`
- 填完之后按 `Ctrl+Alt+V` 可以试听（每按一次换一类情绪），这样不用开游戏也能确认配好没有

### 位置和大小

1. 启动程序，按 **`Ctrl+Alt+T`** 进入调整模式（会出现黄色虚线框）
2. 拖动窗口移动、拖**右下角**缩放
3. 满意之后，把你调的数字抄进配置：

```json
"视图": {
  "缩放": 1.0,        ← 1.0 是"角色完整装进窗口"，调大就放大
  "上下位置": 0.0,     ← 放大之后用这个把脸移进画面（正数向下）
  "左右位置": 0.0
}
```

> 调整模式里的数值会显示在窗口上，照着抄就行。

### 连击与失误

「反应」是共用的一套东西，三样都能填、都能留空：

```json
"连击触发": [
  { "阈值": 50,  "反应": { "瞬时表情": "3.exp3",  "动作": "", "语音情绪": "高兴" } },
  { "阈值": 100, "反应": { "瞬时表情": "4.exp3",  "动作": "", "语音情绪": "高兴" } }
],

"失误触发": {
  "小额门槛": 50,      ← 断连击前手里不到 50 连击 → 什么都不做
  "小额反应": { "瞬时表情": "6.exp3", "动作": "", "语音情绪": "吐槽" },
  "大额门槛": 100,     ← 断连击前手里有 100 以上 → 用大额这一档
  "大额反应": { "瞬时表情": "7.exp3", "动作": "", "语音情绪": "伤心" }
}
```

- `反应` 里三样是**互相独立**的：瞬时表情没播出来语音照样说，语音文件缺了表情照样演
- 不想让某一步发生，就把它留空：`""`
- `动作` 填动作标识（多数成品模型没有额外动作，留空即可）
- **瞬时表情只播一小会儿**，然后会回落到当前的常态表情 ——
  比如 50 连击时冒一下 `3.exp3`，播完回到该区间本该有的那张脸
- 连击阈值想改成 100/200/400/800 这种更稀疏的节奏，改数字就行（一行一个档位）
- 标识写错时**那一项不生效**，画面上会提示"标识在模型里找不到"

### 表现

```json
"表现": {
  "时长策略": "取最长",           ← 「取最长」或「跟随表情」
  "重复触发策略": "保留正在播放",  ← 「保留正在播放」或「切换到新的」
  "表情持续时间毫秒": 1500        ← 瞬时表情播多久
}
```

| 按什么 | 应该看到 |
|---|---|
| 启动后 | 角色显示常态表情（由「常态区间」当前那档决定） |
| `Ctrl+Alt+4` 反复按 | 连击依次推到 10 / 60 / 110 / 170 / 300，**每跨一档角色换一次脸** |
| `Ctrl+Alt+1` | 跨阈值触发一次瞬时表情 → 播完**回落到当前常态表情**（不回待机） |
| `Ctrl+Alt+3` 连按三次 | 依次是：大额失误表情 → 小额失误表情 → **什么都没发生**（门槛以下不反应，这是对的） |
| `Ctrl+Alt+V` | 有语音出声，同时**嘴巴跟着动** |
| `Ctrl+Alt+T` | 出现黄色虚线框，可以拖动和缩放 |
| `Ctrl+Alt+Q` | 程序退出 |
| `调试模式` | `true` = 在画面上显示开发信息，并把日志写到 `%TEMP%\osu-live2d-overlay\debug.log`。**给别人用的时候保持 `false`** |

---

## 装好了吗？对着这个清单验一遍

**不用开游戏**，启动程序后依次按：

| 按什么 | 应该看到 |
|---|---|
| `Ctrl+Alt+1` | 角色换成 50 连击对应的表情 |
| `Ctrl+Alt+3` 连按三次 | 依次是：大额失误表情 → 小额失误表情 → **什么都没发生**（这是对的，门槛以下不反应） |
| `Ctrl+Alt+V` | 有语音出声，同时**嘴巴跟着动** |
| `Ctrl+Alt+T` | 出现黄色虚线框，可以拖动和缩放 |
| `Ctrl+Alt+Q` | 程序退出 |

都正常的话，就可以开游戏试试了。

---

## 出问题了？对照这里

| 你看到的 | 原因 | 怎么办 |
|---|---|---|
| 完全看不到窗口 | 游戏是独占全屏 | 把游戏显示模式切换一次（全屏 ↔ 无边框） |
| 窗口在那儿但是空的 | 模型没加载出来 | 检查 `模型.目录` 和 `模型.入口`；确认那个文件夹里真的有 `.model3.json` |
| 一直白屏或黑屏 | 缺 `live2dcubismcore.min.js` 而且没联网 | 手动下载它，放进 `web\vendor\` 文件夹 |
| 角色出来了但一动不动 | 没配待机动作 | 填 `模型.待机动作`（填**标识**，如 `待机.motion3`）；不填也没关系，只是不动 |
| 连击到了也不切表情 | 标识和模型对不上 | 看画面上的提示（会直接说"标识在模型里找不到"）；把 `调试模式` 打开，日志里有扫描出来的完整清单 |
| 连击完全没反应 | tosu 没连上 | 确认 tosu 已经启动、游戏已经开着；程序状态栏会提示连接情况 |
| 断连击没反应 | 连击没到门槛 | 这是故意的（见上面「连击与失误」）；想改就调低 `小额门槛` |
| 没有声音 | 语音没启用 / 路径不对 / 格式不支持 | 检查 `语音.启用` 是不是 `true`、`语音.目录` 对不对；按 `Ctrl+Alt+V` 试听 |
| 有声音但嘴不动 | 这个模型没有口型参数 | 把 `口型.启用` 改成 `false` 就不折腾了；日志里会说明原因 |
| 窗口挡住了游戏 | 不会的 | 窗口是**点击穿透**的，鼠标会直接穿过它点到游戏上 |
| 想把窗口换位置、改大小 | | 按 `Ctrl+Alt+T` 进调整模式拖动，然后把数值抄进「视图」 |
| 想让程序看着干净点 | 有一堆绿色小框、文字 | 把 `调试模式` 改成 `false`（默认就是 false） |

还搞不定的话：把 `%TEMP%\osu-live2d-overlay\debug.log` 打开看看（需要先把 `调试模式` 设为 `true`
并重启程序），里面会写清楚每一步做到哪、哪一步失败了。

---

## 热键

| 热键 | 作用 |
|---|---|
| `Ctrl+Alt+T` | 进/出调整模式（拖动移动、右下角缩放） |
| `Ctrl+Alt+V` | 试听语音（每按一次换一类情绪） |
| `Ctrl+Alt+1` | 模拟一次连击触发 |
| `Ctrl+Alt+3` | 模拟断连击（连按三次会轮流试三种情况） |
| `Ctrl+Alt+R` | 重载页面 |
| `Ctrl+Alt+Q` | 退出 |

---

## 完成进度

- [x] 悬浮窗：透明、置顶、点击穿透、不抢焦点
- [x] Live2D 渲染，以及运行时模型补丁（不改动你原本的模型文件）
- [x] 连击阈值触发；断连击两档门槛
- [x] 语音系统：与表情解耦、随机不重复、打断式播放
- [x] 口型同步
- [x] 目光跟随鼠标
- [x] 全配置化 + 调试日志
- [ ] **可交互窗口（配置界面 / 数据面板）** —— 现在的悬浮窗是点击穿透的、点不了按钮，
      所以配置界面需要另开一个正常窗口，这是下一步的主要工作
- [ ] 稳定性与易用性：游戏关闭时暂停渲染、托盘图标、首次使用引导
- [ ] **打包分发**：做成"解压双击就能用"，不用装 .NET SDK、不用手改配置
- [ ] 数据可视化：准度、偏移、pp（数据源 tosu 已经在提供，目前只用了连击）

---

## 关于这个项目

- **开发方式**：创意、需求、玩法设计、每一项取舍和验收由作者完成，
  代码在 AI 辅助下编写。这也是它还在早期阶段的原因 —— 会先自己玩过再发出来。
- 只通过 tosu 的接口**只读**读取游戏数据，不改游戏内存、不模拟输入。
- 与 osu! 官方（ppy）无关，不包含也不分发任何游戏素材。
- 运行时会把配置好的动作和表情写进一份**临时副本**来加载，
  不会修改你自己的模型文件。

## 第三方组件

| 组件 | 授权 | 在仓库里 |
|---|---|---|
| [pixi.js](https://github.com/pixijs/pixijs) 6.5.10 | MIT | ✅ |
| [pixi-live2d-display](https://github.com/guansss/pixi-live2d-display) 0.4.0 | MIT | ✅ |
| Live2D Cubism Core | Live2D 专有授权 | ❌ 不进仓库，运行时从官方 CDN 取或本地自备 |

**模型和语音文件都不在本仓库里**，需要自己准备（原因见上面的表格）。